package com.jodexindustries.donatecase.tools.support;

import org.bukkit.entity.Player;

/**
 * PlaceholderAPI plugin support class
 */
public class PAPISupport {
    /**
     * Set up placeholder in string
     * @param player Player for his placeholders
     * @param text String for set up placeholders
     * @return Completed string
     */
    public static String setPlaceholders(Player player, String text) {
        return setPlaceholders(player,text);
    }
}
